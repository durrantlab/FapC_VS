[project]
authors = [
    {name = "FapC_VS", email = "ian.herzogpohl@gmail.com"}
]
description = "test"
name = "FapC_VS"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools"]

[tool.setuptools.packages.find]
where = ["."]
include = ["FapC_VS*"]
