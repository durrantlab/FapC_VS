# 041-setup-pharm-top-div-set
Goal: take the set of molecules similar to top diversity set and set them up with gypsum.
- Input: many similar molecules for each molecule in top div set. Each molecule of div set will have a folder with many SDFs present.
- Output: each molecule in a separate SDF, setup with gypsum

## Data


## Analysis
